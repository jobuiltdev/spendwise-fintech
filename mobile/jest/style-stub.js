// Empty stand-in for stylesheet imports (e.g. `import '@/global.css'` in
// src/constants/theme.ts). Jest has no transformer for CSS, so without this the
// import would fail to parse. Styles carry no behaviour under Jest.
module.exports = {};
